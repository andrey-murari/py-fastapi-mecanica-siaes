[project]
name = "py-fastapi-mecanica-siaes"
version = "0.1.0"
description = "Basic CRUD system for customer"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "psycopg[binary]",
    "SQLAlchemy"
]

[tool.fastapi]         
entrypoint = "main:app"